import React from "react";
import classes from "./Error.module.css";

const Error = () => {
    return (
        <div className={classes.error_back}>
            <h2>Страница не найдена</h2>
            <h2>\(o_o)/</h2>
        </div>
    );
};

export default Error;
